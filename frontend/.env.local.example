# VoiceBook Frontend Environment Configuration
# Local Development: http://127.0.0.1:8000 (preferred over localhost on Windows to avoid IPv6 ::1 resolution drops)
# Production Deployment: https://your-backend-api.onrender.com (or leave empty to use relative /api proxy rewrites)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
